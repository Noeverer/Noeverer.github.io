# Ante Liu 的博客

> 基于 Hexo + Butterfly，通过 GitHub Actions 自动部署

## 🌐 访问地址

**https://noeverer.github.io**

---

## 📝 写作方式

本地只需专注写作，推送到 GitHub 后自动发布。

### 新建文章

```bash
hexo new "文章标题"
```

文章会在 `source/_posts/` 目录下创建，使用 Markdown 编写即可。

### 预览本地效果（可选）

```bash
npm install hexo-cli -g
npm install
hexo server
```

访问 http://localhost:4000

### 发布文章

```bash
git add .
git commit -m "发布新文章"
git push origin master
```

推送到 `master` 分支后，GitHub Actions 会自动构建并部署。

---

## 📂 项目结构

```
Noeverer.github.io/
├── .github/workflows/deploy.yml    # GitHub Actions 自动部署
├── _config.yml                      # Hexo 主配置
├── _config.butterfly.yml            # Butterfly 主题配置
├── source/                          # 博客内容
│   ├── _posts/                      # 所有 Markdown 文章
│   └── about/                       # 关于页面
├── assets/                          # 静态资源（图片、字体等）
├── docs/                            # 项目文档
├── package.json                     # 依赖配置
└── .gitignore                       # Git 忽略规则
```

---

## 🤖 自动部署流程

当代码推送到 `master` 分支时，GitHub Actions 会自动：

1. 安装 Node.js 18 和 npm 依赖
2. 运行 `hexo clean` 和 `hexo generate` 生成静态网站
3. 将生成的 `public/` 目录部署到 `gh-pages` 分支
4. 创建部署标签 `deploy-YYYYMMDD-HHMMSS`

无需手动执行任何部署命令，专注于写作即可。

---

## 📊 内容统计

- **总文章数**: 16篇
- **Chocolate 系列**: 8篇（2015-2019年生活感悟）
- **技术文章**: 5篇（LeetCode + Python总结）
- **思维导图**: 2篇（数据结构、算法）

---

## 🎨 主题信息

- **主题**: [Butterfly](https://github.com/jerryc127/hexo-theme-butterfly) v5.5.3
- **特性**: 响应式设计、暗色模式、代码高亮、图片懒加载、本地搜索

---

## 📖 常用命令

```bash
# 本地预览
hexo server

# 新建文章
hexo new "文章标题"

# 清理缓存（通常不需要）
hexo clean

# 生成静态文件（仅本地测试用，GitHub Actions 会自动执行）
hexo generate
```

---

## 📄 许可证

MIT License

